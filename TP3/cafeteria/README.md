# TP3 - Cafetería ☕

Aplicación React con Vitest y MSW (Mock Service Worker) para simular el sistema de pedidos de una cafetería.

---

## 🚀 Requisitos
- Node.js 18 o superior

---

## 📂 Instalación
Ubicate en la carpeta raíz del proyecto (por ejemplo:  
`C:\Users\Taller\Desktop\Prog4\TP3\cafeteria`) y ejecutá:

```bash
npm install

Ejecutar el proyecto
Desde la misma carpeta raíz (cafeteria):
npm run dev

Ejecutar los tests
Desde la misma carpeta raíz (cafeteria):
npm run test


Configuración de MSW (solo la primera vez)
Si es la primera vez que corrés el proyecto, ejecutá:
npx msw init public --save
